# D1 — Real-DEX swapper

> **Statement of Work, Deliverable 1.** Integrate the existing swapper contract end to end so
> that it performs a real swap through the Soroswap testnet router, expose it in the visual
> builder, and produce a verifiable swap transaction. Full wording in the
> [Statement of Work](../../instawards-phase-1-sow.md), section 4.1.

**Target week:** 1 (7–13 September) · **Status:** In progress — contract, pipeline and builder
merged; simulation preview in review; testnet deploy-and-trigger not started. Updated 9 September.

## What a user can do when this is done

Open [paiflow.xyz](https://paiflow.xyz), drag a **Swap** block onto the canvas, choose the
asset going in and the asset coming out, set a maximum slippage, deploy to Stellar testnet,
and send funds in. The contract swaps them on a real decentralised exchange and forwards the
result to the next step in the flow. Every step is verifiable on stellar.expert.

## Where it stands on 9 September

- **Merged to `develop`:** the swapper contract calling the Soroswap router (7 Sep), the
  pipeline plumbing that compiles a swap block into it (7 Sep), and the builder work that makes
  the block visible and configurable (9 Sep). The Swap block exists end to end in the code.
- **In review:** the simulation preview — a live Soroswap quote in the config panel and on the
  deploy review. Verified locally against the real testnet router: 10 XLM quotes at ~1.056 USDC
  with a 1 % minimum of ~1.049 USDC.
- **Verified in a browser for the first time on 9 September.** The builder spec — palette, panel,
  five edge-case errors, deploy review with the TESTNET chip and the live quote — passes 9/9 on
  a local environment. The screenshots and API samples it produced are captured and will be
  published with the deploy-and-trigger evidence.
- **Not started:** deploying a swap flow from the app and triggering it to produce the swap
  transaction the SOW asks for. The one unknown — a contract swapping its own balance with
  `to = self` — was settled on 6 September on a verification copy (standalone swapper
  `CDEEJZG6…DOOU`, [`5389cdee…`](https://stellar.expert/explorer/testnet/tx/5389cdee87826b944f4fca397c0fadc8524cbb2429c2a657a306a06a4c5fc673)). That transaction proves the contract; it was not produced from
  paiflow.xyz and does not count toward the metrics.
- **Found on the way:** on a fresh machine the contract deploy chain never deployed a factory,
  so nothing could deploy. Reproduced, fixed, and re-verified on 9 September; the fix is in
  review. Compiling actions in canvas-insertion order rather than topological order was also
  found; a fail-closed guard is merged and the real fix is tracked separately.

## Traceability

Each clause of the SOW mapped to the change that implements it and the evidence that proves
it. Issue numbers are internal references; the commit and evidence links are public.

| SOW clause                                                             | Issue | Change                                                                 | Evidence                                                                                                                                              | Status      |
| ---------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Call `swap_exact_tokens_for_tokens` on the Soroswap testnet router     | #387  | [`3315ea3`](https://github.com/artisam-paiflow/paiflow/commit/3315ea3) | [`5389cdee…`](https://stellar.expert/explorer/testnet/tx/5389cdee87826b944f4fca397c0fadc8524cbb2429c2a657a306a06a4c5fc673) (verification copy, 6 Sep) | Done        |
| Add `execute_step(asset, amount)`                                      | #387  | [`3315ea3`](https://github.com/artisam-paiflow/paiflow/commit/3315ea3) | contract tests in `contracts/actions/swapper`                                                                                                         | Done        |
| Compute `amount_out_min` from `slippage_bps`                           | #387  | [`3315ea3`](https://github.com/artisam-paiflow/paiflow/commit/3315ea3) | spot-price bound asserted in contract and unit tests                                                                                                  | Done        |
| Update the constructor arguments                                       | #387  | [`3315ea3`](https://github.com/artisam-paiflow/paiflow/commit/3315ea3) | `router`, `deadline_secs`, `next_steps` in the constructor                                                                                            | Done        |
| Stop mapping swap flows to the splitter template kind                  | #388  | [`3a76e93`](https://github.com/artisam-paiflow/paiflow/commit/3a76e93) | `tests/unit/swapper-pipeline.test.ts`                                                                                                                 | Done        |
| Fix the swapper constructor serialization                              | #388  | [`3a76e93`](https://github.com/artisam-paiflow/paiflow/commit/3a76e93) | `tests/unit/swapper-pipeline.test.ts`                                                                                                                 | Done        |
| Flow validation rules for the swap node                                | #388  | [`3a76e93`](https://github.com/artisam-paiflow/paiflow/commit/3a76e93) | unit cases; [e2e capture set](../evidence/README.md)[^local] 03–06                                                                                    | Done        |
| Swap event parsing and live-event classification                       | #388  | [`3a76e93`](https://github.com/artisam-paiflow/paiflow/commit/3a76e93) | unit tests in `tests/unit/`                                                                                                                           | Done        |
| Unhide the swapper block in the builder palette                        | #389  | `b8fd9b8`[^mirror]                                                     | [e2e capture set](../evidence/README.md)[^local] 01                                                                                                   | Done        |
| Build the Swapper configuration panel                                  | #389  | `b8fd9b8`[^mirror]                                                     | [e2e capture set](../evidence/README.md)[^local] 02                                                                                                   | Done        |
| Edge-case validation errors display clearly in the UI                  | #389  | `b8fd9b8`[^mirror]                                                     | [e2e capture set](../evidence/README.md)[^local] 03–06                                                                                                | Done        |
| Swapper simulation preview                                             | #391  | PR #403, in review                                                     | [e2e capture set](../evidence/README.md)[^local] 07–10, live testnet quote                                                                            | In progress |
| Deploy and trigger a swapper flow on testnet, producing a swap tx hash | #390  | stage 1 proven; stage 2 not started                                    | [`5389cdee…`](https://stellar.expert/explorer/testnet/tx/5389cdee87826b944f4fca397c0fadc8524cbb2429c2a657a306a06a4c5fc673) (stage 1 only)             | In progress |

[^mirror]: Merged to `develop` on 9 September; the public mirror has not been synced past 7 September yet, so this commit has no public link at the time of writing.

[^local]: Captured on 9 September from a local run of `tests/e2e/swap-panel.spec.ts` — seven screenshots and three API samples. They are held until the deploy-and-trigger run so the evidence pack is published in one piece and from the public app.

## Evidence required

The SOW asks for four things for this deliverable (section 6.1). All four must be openable by
someone with no account.

- [ ] **Live app URL** — the Swap block visible and configurable at [paiflow.xyz](https://paiflow.xyz). _Merged to `develop`; not yet deployed to the public app._
- [ ] **Screen recording** — deploying _and_ triggering a swapper flow from the builder. _Not started._
- [ ] **Transaction hash on stellar.expert** — a swap executed through the Soroswap router. _Stage-1 proof exists ([`5389cdee…`](https://stellar.expert/explorer/testnet/tx/5389cdee87826b944f4fca397c0fadc8524cbb2429c2a657a306a06a4c5fc673)) from the verification copy; the flow-driven swap from paiflow.xyz is not started._
- [ ] **Contract WASM hash** — the swapper binary, resolvable on testnet. _Uploaded: `e9482ff07fcf4791aa3f8deebeda6159081a04f13e8ac63c28e91b7f80b23d1a`; explorer link to be added to the evidence index._

The recording should also show the two things the weekly plan calls for: a validation error
displaying clearly when the configuration is wrong, and the simulation preview showing the
expected output before deployment. Both are already captured as stills in the local e2e set.

## Scope notes

Three points where the engineering work is narrower or wider than the SOW's wording. They are
recorded here rather than by editing the SOW, which is fixed as approved.

**The router is not user-selectable.** The SOW says "router selector". The panel shows a
single, read-only option, and the actual address is supplied by the server at deployment time.
A free-text address field would let a user point a flow at an arbitrary contract, and every
existing flow would break the next time Soroswap redeploys its testnet router.

**No new asset type.** The swapper trades the assets Paiflow already supports, using the live
XLM / USDC pool. Supporting exchange-issued tokens would have touched the asset model, payment
links and notifications, and was judged too wide for a 30-day sprint.

**One output.** A swap block has at most one downstream step. Splitting a payment several ways
is the splitter block's job, and combining the two is a normal flow.
