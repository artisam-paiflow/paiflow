# D3 — Shared builder inputs

> **Statement of Work, Deliverable 3.** Create reusable builder input components and adopt
> them in the Swapper configuration panel, with unified validation and error rendering,
> component tests, and an accessibility and mobile pass. Full wording in the
> [Statement of Work](../../instawards-phase-1-sow.md), section 4.1.

**Target week:** 3 (21–27 September) · **Status:** Complete. All four evidence items the SOW
asks for exist and are public: the rebuilt panel is live on paiflow.xyz, the screen recording is
on Google Drive, and the side-by-side screenshots and the CI test report are in the evidence pack.
Every SOW clause below maps to merged, tested code. The week-3 report is still to be written.

## What a user can do when this is done

The Swapper panel is built from the four shared inputs the SOW names, each with the same
labelling, the same validation and the same error messages. Two asset selects, `Asset In` and
`Asset Out`, pick the pair. A share input, `Max slippage (%)`, sets the maximum slippage. Under
**Preview**, an amount input, `You send`, sets the size the live quote is asked for, and the quote
shows the expected output as its figure with the minimum the contract will accept underneath. A
collapsed **Advanced** section holds the address picker, which shows the Soroswap router the flow's
funds pass through (shown, not chosen — the server pins it), and the deadline. It opens by itself
when the deadline has an error or the router is not configured, so a problem is never hidden. The
panel is usable with a keyboard and on a phone.

## Traceability

Each clause of the SOW mapped to the change that implements it and the evidence that proves it.
Issue numbers are internal references; the commit and evidence links are public. A commit link is
the merge of the pull request that made the change, so it covers every commit in that pull request.

| SOW clause                                                                                                               | Issue                                               | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                         | Status    |
| ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| Design shared input components (`AssetSelect`, `AmountInput`, `AddressPicker`, `ShareInput`) (§4.1 l.244)                | #607, #608, #609, #648, #661                        | [`decf7e4`](https://github.com/artisam-paiflow/paiflow/commit/decf7e49507a9902759bbbeef85f5edd507babff) `Field` and `AssetSelect`; [`4bb8fde`](https://github.com/artisam-paiflow/paiflow/commit/4bb8fded6fcecfb54b6c6054a9774f1aa4d72b26) `AmountInput` and `ShareInput`; [`945c627`](https://github.com/artisam-paiflow/paiflow/commit/945c6271c50694d1f1bc7292758d6bb9e7bc4d8c) `AddressPicker`; [`0219f12`](https://github.com/artisam-paiflow/paiflow/commit/0219f128ed17524e360e88827c6aa5b6f3a24a84), [`c28cb8c`](https://github.com/artisam-paiflow/paiflow/commit/c28cb8cc3d918612416350a8a7b70e939d515101) its editable branch on the shared styles, with named 44px controls                                                                                                                                                                                                                                                                                        | `tests/unit/builder/inputs/*.test.tsx` in [`21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml); the [props contract](#props-contract)                                                                                                                                                                                                                                                                     | Evidenced |
| Adopt them in the Swapper config panel (§4.1 l.244; §3.8 l.200–201, "Swapper config panel uses shared input primitives") | #611, #612, PR #670                                 | [`fdb04da`](https://github.com/artisam-paiflow/paiflow/commit/fdb04daa3c1435673563314b8a5d1c736bf1f870) extract the panel, no behaviour change; [`cdbb8ee`](https://github.com/artisam-paiflow/paiflow/commit/cdbb8ee3242caa04cb33f7b97a3e3f289d9046d3) rebuild it on the shared inputs; [`5a866ae`](https://github.com/artisam-paiflow/paiflow/commit/5a866ae46ba2b56975a1e2c3ba79fb35fc9ca15c) the simplified layout (see scope notes 1, 2 and 14)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tests/unit/builder/panels/swap-panel.test.tsx`; [`06`](../evidence/d3/06-swap-panel-after.png), [`16`](../evidence/d3/16-swap-panel-advanced-after-desktop.png), [`17`](../evidence/d3/17-swap-panel-before-after.png); the [recording](https://drive.google.com/file/d/1bcZP0jpYhn_9kdQzs73kr01DpXBeXn2Y/view?usp=sharing), 0:31–2:50                                                                          | Evidenced |
| Unify Zod validation schemas and error rendering for the Swapper flow (§4.1 l.244)                                       | #610, #607, #646, #649                              | [`e3ba1dc`](https://github.com/artisam-paiflow/paiflow/commit/e3ba1dc78875f9498613c62f1d2848be3b9c87b9) shared primitives, one rule function, one error model; [`decf7e4`](https://github.com/artisam-paiflow/paiflow/commit/decf7e49507a9902759bbbeef85f5edd507babff) `Field` renders every error the same way; [`d4bacbf`](https://github.com/artisam-paiflow/paiflow/commit/d4bacbf037fca056c1fbe3bb89be9b1b2eb5364e) one path normaliser; [`a976d78`](https://github.com/artisam-paiflow/paiflow/commit/a976d780a4a8e3dd050b572bde5ba6e07e6d49db) same-asset refusal proven at the prepare route (see scope note 3)                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/flows/swap-rules.test.ts`, `asset-catalogue.test.ts`, `tests/unit/api/deploy-prepare-swap.test.ts`; [`04`](../evidence/d3/04-keyboard-error-desktop.png), [`15`](../evidence/d3/15-slippage-error-desktop.png); the [code-diff note](../evidence/d3/20-code-diff.md)                                                                                                                                 | Evidenced |
| Build the components so they can be reused in other node types in future sprints (§4.1 l.244)                            | #607, #608, #609                                    | [`decf7e4`](https://github.com/artisam-paiflow/paiflow/commit/decf7e49507a9902759bbbeef85f5edd507babff), [`4bb8fde`](https://github.com/artisam-paiflow/paiflow/commit/4bb8fded6fcecfb54b6c6054a9774f1aa4d72b26), [`945c627`](https://github.com/artisam-paiflow/paiflow/commit/945c6271c50694d1f1bc7292758d6bb9e7bc4d8c)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | One test per component with a non-swap props shape (listed under [props contract](#props-contract))                                                                                                                                                                                                                                                                                                              | Done      |
| Add component tests (§4.1 l.244; §5.1 Week 3 l.285)                                                                      | #606; tests in #607–#609, #612                      | [`44928ca`](https://github.com/artisam-paiflow/paiflow/commit/44928ca744f62a7b6f588d992ac5a85bbe3ecd93) a jsdom `dom` project beside the node suite, Testing Library, axe                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Seven `.test.tsx` files, 83 tests, in [`21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml)                                                                                                                                                                                                                                                                                                                | Evidenced |
| Accessibility and mobile viewport pass (§4.1 l.244; §5.1 Week 3 l.285)                                                   | #607–#609, #613, #647, #648, #661, PR #677, PR #679 | [`a494e86`](https://github.com/artisam-paiflow/paiflow/commit/a494e862bdd8298052d3acdce0211c3cab43d9c5) the config panel container: keyboard open, Close, focus return, a docked sheet below 768px; [`0617084`](https://github.com/artisam-paiflow/paiflow/commit/0617084de09148c2fa6f9c107cd23bee90bbee3e) reduced motion on the canvas edges; [`0219f12`](https://github.com/artisam-paiflow/paiflow/commit/0219f128ed17524e360e88827c6aa5b6f3a24a84), [`c28cb8c`](https://github.com/artisam-paiflow/paiflow/commit/c28cb8cc3d918612416350a8a7b70e939d515101); [`56a3c58`](https://github.com/artisam-paiflow/paiflow/commit/56a3c58122c984239817bc2ed5a615f295919434) a focus ring on canvas nodes and edges, and Skip to canvas; [`e192dc9`](https://github.com/artisam-paiflow/paiflow/commit/e192dc912b76294fed5d20b0ba90540887032c32) the builder chrome on phones, code only: no capture or recording was taken on a build that has it (see scope notes 7, 12 and 15) | `tests/e2e/d3-swap-panel.spec.ts` on desktop and Pixel 7; [`03`](../evidence/d3/03-keyboard-open-desktop.png), [`07`](../evidence/d3/07-docked-sheet-mobile.png), [`08`](../evidence/d3/08-docked-sheet-scrolled-mobile.png), [`13`](../evidence/d3/13-phone-keyboard-up.jpg) (a real phone); the [recording](https://drive.google.com/file/d/1bcZP0jpYhn_9kdQzs73kr01DpXBeXn2Y/view?usp=sharing), 5:44 and 6:38 | Evidenced |
| Week 3: "UX pass" (§5.1 l.285)                                                                                           | #612, #613                                          | [`cdbb8ee`](https://github.com/artisam-paiflow/paiflow/commit/cdbb8ee3242caa04cb33f7b97a3e3f289d9046d3) a clamp announced on blur, each error on its own field, the real router id, the quote at the user's size; [`a494e86`](https://github.com/artisam-paiflow/paiflow/commit/a494e862bdd8298052d3acdce0211c3cab43d9c5) keyboard open, Close, focus return; [`5a866ae`](https://github.com/artisam-paiflow/paiflow/commit/5a866ae46ba2b56975a1e2c3ba79fb35fc9ca15c) one-line hints and the quote as a figure                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Before → after pairs `10`→`15`, `12`→`16`, `14`→ the [recording](https://drive.google.com/file/d/1bcZP0jpYhn_9kdQzs73kr01DpXBeXn2Y/view?usp=sharing) at 5:44 ([evidence pack](../evidence/d3/README.md))                                                                                                                                                                                                         | Evidenced |
| §3.2 l.108: "consistent config-panel inputs and validation messages in the Swapper panel"                                | #610, #612                                          | [`e3ba1dc`](https://github.com/artisam-paiflow/paiflow/commit/e3ba1dc78875f9498613c62f1d2848be3b9c87b9), [`cdbb8ee`](https://github.com/artisam-paiflow/paiflow/commit/cdbb8ee3242caa04cb33f7b97a3e3f289d9046d3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | [`04`](../evidence/d3/04-keyboard-error-desktop.png), [`15`](../evidence/d3/15-slippage-error-desktop.png); the [recording](https://drive.google.com/file/d/1bcZP0jpYhn_9kdQzs73kr01DpXBeXn2Y/view?usp=sharing), 0:31–2:20                                                                                                                                                                                       | Evidenced |
| §3.7(c) l.176–177: inputs standardised "without breaking existing templates"                                             | #611, #609, #607, #613, #610                        | [`fdb04da`](https://github.com/artisam-paiflow/paiflow/commit/fdb04daa3c1435673563314b8a5d1c736bf1f870) extract with no behaviour change; [`945c627`](https://github.com/artisam-paiflow/paiflow/commit/945c6271c50694d1f1bc7292758d6bb9e7bc4d8c) `AddressPicker`'s existing call sites unchanged; [`decf7e4`](https://github.com/artisam-paiflow/paiflow/commit/decf7e49507a9902759bbbeef85f5edd507babff) the legacy `Field` and `.input` untouched; [`a494e86`](https://github.com/artisam-paiflow/paiflow/commit/a494e862bdd8298052d3acdce0211c3cab43d9c5) desktop behaviour asserted unchanged; [`e3ba1dc`](https://github.com/artisam-paiflow/paiflow/commit/e3ba1dc78875f9498613c62f1d2848be3b9c87b9) issue paths unchanged                                                                                                                                                                                                                                              | `tests/e2e/swap-panel.spec.ts`; the node suite in [`21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml); [`18`](../evidence/d3/18-pay-panel-legacy-desktop.png), a panel still on the legacy inputs                                                                                                                                                                                                        | Done      |
| Week 3: "Validation errors display consistently in the Swapper panel" (§5.1 l.285)                                       | #610, #612                                          | [`e3ba1dc`](https://github.com/artisam-paiflow/paiflow/commit/e3ba1dc78875f9498613c62f1d2848be3b9c87b9), [`cdbb8ee`](https://github.com/artisam-paiflow/paiflow/commit/cdbb8ee3242caa04cb33f7b97a3e3f289d9046d3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tests/unit/builder/panels/swap-panel.test.tsx`; [`04`](../evidence/d3/04-keyboard-error-desktop.png), [`15`](../evidence/d3/15-slippage-error-desktop.png)                                                                                                                                                                                                                                                      | Evidenced |
| Week 3: "Shared inputs pass smoke tests" (§5.1 l.285)                                                                    | #606, #607, #608, #609                              | [`44928ca`](https://github.com/artisam-paiflow/paiflow/commit/44928ca744f62a7b6f588d992ac5a85bbe3ecd93), [`decf7e4`](https://github.com/artisam-paiflow/paiflow/commit/decf7e49507a9902759bbbeef85f5edd507babff), [`4bb8fde`](https://github.com/artisam-paiflow/paiflow/commit/4bb8fded6fcecfb54b6c6054a9774f1aa4d72b26), [`945c627`](https://github.com/artisam-paiflow/paiflow/commit/945c6271c50694d1f1bc7292758d6bb9e7bc4d8c)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/unit/builder/inputs/smoke.test.tsx` in [`21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml)                                                                                                                                                                                                                                                                                                        | Evidenced |
| Week 3: "UI tests green in CI" (§5.1 l.285)                                                                              | #606                                                | [`44928ca`](https://github.com/artisam-paiflow/paiflow/commit/44928ca744f62a7b6f588d992ac5a85bbe3ecd93) a junit report, an uploaded artifact and a job summary (see scope notes 5, 6 and 11)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [CI run 35982489259](https://github.com/artisam-paiflow/paiflow/actions/runs/35982489259); [`21-ci-summary.png`](../evidence/d3/21-ci-summary.png)                                                                                                                                                                                                                                                               | Evidenced |
| §6.1 l.299: live app URL, screen recording, side-by-side before-and-after screenshots, CI test report                    | #636, #637, #639, #640                              | Promoted to paiflow.xyz; captures, recording and CI report added to the evidence pack                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [Evidence required](#evidence-required)                                                                                                                                                                                                                                                                                                                                                                          | Evidenced |

## Evidence required

Section 6.1 of the SOW asks for four things:

- [x] **Live app URL** — the rebuilt panel at [paiflow.xyz](https://paiflow.xyz), live since
      23 September. Since 25 September the app has served `staging` at
      [`8c008c0`](https://github.com/artisam-paiflow/paiflow/commit/8c008c01ae80a485e30456fe9519ad69289e19a9),
      which carries every change in the traceability table above.
- [x] **Screen recording** — building a swapper flow with the new inputs:
      [Google Drive](https://drive.google.com/file/d/1bcZP0jpYhn_9kdQzs73kr01DpXBeXn2Y/view?usp=sharing), recorded on 25 September; record in
      [`d3/19-recording-run.json`](../evidence/d3/19-recording-run.json)
- [x] **Before-and-after screenshots** of the Swapper panel, side by side:
      [`d3/17-swap-panel-before-after.png`](../evidence/d3/17-swap-panel-before-after.png), which
      puts `09` (the D1 panel, 22 September) beside `16` (the rebuilt panel with Advanced open,
      25 September). The [D3 evidence pack](../evidence/d3/README.md) pairs every "before" with the
      "after" that answers it. _The "before" half is the panel this deliverable replaces — the one
      D1 shipped: [`d1/02-swap-panel-after.png`](../evidence/d1/02-swap-panel-after.png) and
      [`d1/08-swap-panel-live-quote.png`](../evidence/d1/08-swap-panel-live-quote.png), from
      paiflow.xyz on 9 September, plus `09`–`14`, the same panel on 22 September before D3 was
      promoted: keyboard focus, slippage typing, the same-asset error, the router, and the keyboard
      opening and closing the panel (no phone "before" was captured)._
- [x] **CI test report** — component tests passing:
      [public mirror CI run 35982489259](https://github.com/artisam-paiflow/paiflow/actions/runs/35982489259),
      committed as [`d3/21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml) (121 files, 1756
      tests, 83 of them in the component project), with the job summary
      [`21-ci-summary.png`](../evidence/d3/21-ci-summary.png) and the run's details in
      [`21-ci-meta.json`](../evidence/d3/21-ci-meta.json)

{% hint style="info" %}
**Which screenshot is the "before".** D3 rebuilds the panel that D1 shipped, so D3's "before" is
D1's "after": `Asset In`, `Asset Out`, a read-only router, `Max slippage (%)`, `Deadline (seconds)`
and a live quote, on the builder's original hand-rolled inputs. The three older files under
[evidence](../evidence/README.md#screenshots-and-recordings) — `d3/00-palette-before.png`,
`d3/01-builder-before.png` and `d3/02-swap-panel-before.png` — were taken on 9 September while
paiflow.xyz still ran the **pre-D1** panel (a raw
`Rate (basis points, 1–10000)` field, with no slippage, no deadline and no router). They are kept
as the pre-sprint baseline and are D1's "before", not D3's: the fields that differ between them and
today's panel are D1's work and are not claimed here.
{% endhint %}

## Scope notes

Fifteen points where the work differs from the SOW's wording, goes beyond it, or needs stating
next to it. **Three depart from the wording (1–3). Each is minor, and each narrows risk rather than
scope:** all four named components are rendered in the Swapper panel, every D3 clause has an owner,
and nothing the SOW asks for is dropped. The rest are not departures. They are hardening or evidence
added beyond the SOW, the SOW's own out-of-scope line (§4.1 l.262–263) applied, or clauses met
literally and recorded for clarity. Each was checked against the code before the sprint and
re-audited on 21, 22 and 25 September, and they are recorded here rather than by editing the SOW,
which is fixed as approved. Where the reason is security it says so; where it is not, it says what it is.

Type: **Deviation** differs from the SOW's wording · **Added** goes beyond what the SOW asks ·
**Scope line** applies the SOW's own exclusion · **Note** is met literally, recorded for clarity.

**1. The router is shown, not chosen.** _Deviation, carried from D1._ The SOW asks for
`AddressPicker` in the Swapper panel (§4.1 l.244) and for the builder to "select … `router`" (§3.2
l.103–104). `AddressPicker` is adopted in a read-only pinned mode, under Advanced, that shows the
router's contract id with a copy button and a stellar.expert link. The graph has no router field;
the server supplies the router at deploy time (`lib/stellar/deploy.ts:155-162`). _Why — security:_
an editable router would let a flow send its funds through an arbitrary contract. D1 recorded the
same choice ([D1 scope notes](d1.md#scope-notes)), and D3 does not reopen it to give a component
somewhere to live. Showing the real contract id is more transparent than the D1 panel's disabled
one-option select (`d3/12`). _Verify:_ `d3/16`; `tests/unit/swapper-pipeline.test.ts:260`, "…leaves
the router off the graph"; no `router` key in `SwapAction` (`lib/flows/schema.ts:364-380`).

**2. Slippage is the share input, and the amount input sizes the quote and stores nothing.**
_Deviation, minor._ The SOW asks for `AmountInput` and `ShareInput` in the Swapper panel (§4.1
l.244). Both are rendered. `ShareInput` is `Max slippage (%)`, a percentage stored as basis points;
its share-of-total remainder line is off, because a swap has one output. `AmountInput` is
`You send` under Preview: it sets the size of the live quote and is UI state only
(`components/builder/panels/swap-panel.tsx:41-43`). _Why — security and stability, the contract is
not touched:_ the swapper swaps whatever arrives and has one output, so a stored amount or share
would be a constructor change, a breaking ABI change (CLAUDE.md §8) that would replace the WASM hash
D1 evidenced and reopen the contract D1 validated on testnet. Quoting at the user's own size is
also a safety gain: price impact grows with size, so a quote fixed at 10 units can look healthy for
a flow whose real deposits break a tight slippage bound (pool reserves on 21 September: 3,941,589
XLM / 416,520 USDC). _Verify:_ the four imports at `swap-panel.tsx:8-12`;
`tests/unit/builder/panels/swap-panel.test.tsx:287`, "keeps the preview amount out of the graph";
the recording at 2:20; no D3 commit touches `contracts/` or `SwapAction`'s fields.

**3. The two cross-field swap rules live in the deploy gate, not in a Zod refinement.** _Deviation,
narrow._ The SOW says "unify Zod validation schemas" (§4.1 l.244). This is met for the schemas: the
swapper flow's Zod schemas (`SwapAction.slippageBps`, the quote query, the builder trigger amount)
share one set of primitives in `lib/flows/primitives.ts`. The exception is the two rules that
compare fields, that the assets differ and that slippage clears the pool fee. They stay in
`validateFlow`, written once in `swapConfigIssues()` (`lib/flows/swap-rules.ts:41`, called at
`lib/flows/validate.ts:704`), rather than in a `superRefine` on `SwapAction`. _Why — reliability,
not security, because it prevents lost edits:_ `PATCH /api/flows/[id]` parses the graph before it
saves (`app/api/flows/[id]/route.ts:31`), so a cross-field rule inside the Zod shape would 422 every
autosave while the user is mid-edit and drop their other edits. The rules lose nothing at the gate:
the server enforces them at `/api/flows/:id/validate` and again inside `/api/deployments/prepare`,
which every deploy passes. _Verify:_ `tests/unit/flows/swap-rules.test.ts:143`, where a same-asset
swap parses and is refused by `validateFlow`; `tests/unit/api/deploy-prepare-swap.test.ts:167`,
where the prepare route refuses it with a field error on `Asset Out` and prepares nothing (#649).

**4. The server enforces the asset catalogue the panel offers.** _Added._ The SOW implies the
panel restricts which assets a swap can use (implicit). One catalogue now feeds both `AssetSelect`
and a `validateFlow` rule for swap nodes, and a `PENDING:` issuer is a field error. _Why — security:_
before the sprint the restriction was client-only. The API and the AI editor could save a swap on an
arbitrary issuer's token, whose SAC address is then fixed in an immutable constructor, and a
`PENDING:` issuer made the prepare route throw a 500 and mark the deployment `FAILED`. _Verify:_
`tests/unit/flows/swap-rules.test.ts:52` and `:61`; `tests/unit/flows/asset-catalogue.test.ts:25`;
`tests/unit/api/deploy-prepare-swap.test.ts:103`, a 422 with a field path, not a 500.

**5. The component tests run in CI; the accessibility and phone spec runs outside it.** _Note._
The SOW asks for component tests and "UI tests green in CI" (§5.1 l.285). This is met literally:
the jsdom component tests run in CI's node lane. The axe and Pixel 7 Playwright spec, the SOW's
accessibility and mobile _pass_, which it does not ask to be a CI job, runs locally and against
paiflow.xyz. _Why — no end-to-end lane exists (CLAUDE.md §12, §19),_ and building one (Postgres,
Redis, a seeded admin, a dev server, with `happy-path.spec.ts` already broken, #447) is its own
concern. The four new packages are devDependencies only, never reach the shipped bundle, and
`pnpm audit` stayed as it was. _Verify:_ the `dom` project in
[`21-vitest-junit.xml`](../evidence/d3/21-vitest-junit.xml); `tests/e2e/d3-swap-panel.spec.ts`.

**6. CI publishes a test report on every run.** _Added._ The SOW asks for a "CI test report" (§6.1
l.299). CI now writes a junit report, uploads it as the `vitest-junit` artifact and adds a job
summary on every run. _Why — stronger evidence:_ D1 and D2 evidenced CI as a sentence; this is
recorded only because it adds a step to CI. _Verify:_ `.github/workflows/ci.yml:45-55`; the summary
in [`21-ci-summary.png`](../evidence/d3/21-ci-summary.png).

**7. The accessibility pass covers the panel container, not the mobile canvas.** _Scope line._ The
SOW asks for an "accessibility/mobile viewport pass" of the Swapper panel (§4.1 l.244). The pass
covers the shared inputs **and** the config panel container: keyboard open, Close, focus return,
reduced motion, and a docked sheet below 768px. _Why — the container is shared by every node type,_
so that part of the fix is global by necessity: a panel that cannot be reached cannot pass. The
mobile builder as a whole is #62 and is the "full builder-wide UI refactoring" §4.1 l.262 excludes.
Two later changes touched the builder around the panel without taking #62 on: a visible focus ring
on canvas nodes and edges with a Skip to canvas button (#677), and a one-row header, a bottom-right
Ask AI button and a hidden MiniMap on phones (#679). Palette items were already tap-to-add buttons
(`components/builder/palette.tsx:360-362`). _Verify:_ `tests/e2e/d3-swap-panel.spec.ts` on both
Playwright projects.

**8. The components are adopted in the Swapper panel only; shared plumbing is fixed once.**
_Scope line._ The SOW describes builder inputs that "are not standardized … creating inconsistent
validation, duplicated UI code" across node types (§3 item 3, l.70–72). The components are adopted
in the Swapper panel only. Four pieces of shared plumbing the swapper flow passes through are fixed
once rather than forked for the swapper: the issue flattening in four routes and the builder trigger
route's amount check (#610), the config panel container (#613), and `AddressPicker`'s file, whose
existing callers keep their behaviour (#609). _Why — the SOW's own out-of-scope line (§4.1
l.262–263), and efficiency:_ a swapper-only copy of shared code would be the duplication this
deliverable exists to remove. What the survey found elsewhere is filed, not absorbed (#615–#620,
#663). _Verify:_ `issuesToFields` in `app/api/deployments/prepare`, `app/api/flows`,
`app/api/flows/[id]/edit` and `app/api/flows/[id]/resolve-addresses`; the unchanged-behaviour
assertions in [`945c627`](https://github.com/artisam-paiflow/paiflow/commit/945c6271c50694d1f1bc7292758d6bb9e7bc4d8c),
[`fdb04da`](https://github.com/artisam-paiflow/paiflow/commit/fdb04daa3c1435673563314b8a5d1c736bf1f870)
and [`a494e86`](https://github.com/artisam-paiflow/paiflow/commit/a494e862bdd8298052d3acdce0211c3cab43d9c5).

**9. The evidence adds the code diff and an unmigrated panel to the screenshots.** _Added._ The SOW
asks for side-by-side before-and-after screenshots (§6.1 l.299). The pack adds the change shown in
code and a panel that has not been migrated. _Why — a screenshot pair cannot show
standardisation:_ the same panel looks similar at a glance (see
[below](#what-before-and-after-means-here)). _Verify:_
[`20-code-diff.md`](../evidence/d3/20-code-diff.md) and
[`18-pay-panel-legacy-desktop.png`](../evidence/d3/18-pay-panel-legacy-desktop.png).

**10. The builder validates the graph once per change, not twice.** _Added._ The SOW does not ask
for this (implicit: validation runs wherever it is needed). `validateFlow` now runs once per graph
change, and `ConfigPanel` receives the issues as a prop. _Why — efficiency:_ before the sprint a
full `validateFlow` plus `flowToPipeline` ran twice on every graph change, which is every keystroke
in a field. _Verify:_ one `validateFlow(` call under `components/builder/`
(`builder-client.tsx:291`); the code-diff note.

**11. The CI report is kept where a reviewer can open it.** _Added._ The SOW asks for a "CI test
report" (§6.1 l.299). The report comes from a run on the public mirror, and its junit XML and a
capture of its summary are committed to the evidence pack. _Why — durability:_ CI on the
development repository is private, and Actions artifacts expire after about 90 days (this one on
23 December), the same reasoning as D2's saved `getTransaction` JSON. _Verify:_ the XML's 1756 tests match the run's summary
([`21-ci-meta.json`](../evidence/d3/21-ci-meta.json)).

**12. The accessibility claim is the Swapper panel's, not the whole builder's.** _Scope line._ The
SOW asks for an "accessibility/mobile viewport pass" (§4.1 l.244). The axe scan is scoped to the config panel container on the Swapper panel. The unmigrated panels keep
the legacy unlabelled `Field`. _Why — §4.1 l.262–263;_ stated so the accessibility claim is not read
as builder-wide. axe also passes a control named only by a `title`, and its target-size rule is off
by default, so the axe result is not evidence of names or touch targets; the 44px scan is.
_Verify:_ the axe scope in `tests/e2e/d3-swap-panel.spec.ts`; the unmigrated Pay panel
([`18`](../evidence/d3/18-pay-panel-legacy-desktop.png)).

**13. A trigger that carries a custom asset can no longer feed a swap.** _Added, a consequence of
note 4._ The SOW does not address custom assets on a swap (implicit). There is still no
server-side check that a pool exists. _Why — security over reach:_
XLM/USDC is the only pair Paiflow has verified on the pinned router and the only one the panel has
ever offered, so nothing the builder can produce is lost. A swap saved through the API with another
asset stops deploying; on 22 September the public app held **0** such saved flows. A pool check
needs an RPC read inside validation and is #618; the live quote stays the user-facing signal.
_Verify:_ the friendly message names the upstream asset as the cause
(`tests/unit/flows/swap-rules.test.ts:67`).

**14. The deadline is a fifth field with no shared component.** _Note._ The SOW names four
components for the Swapper panel (§4.1 l.244), and the panel renders all four. It also has
`Deadline (seconds)`, a `DeadlineInput` that belongs to the panel (`swap-panel.tsx:144`, used at
`:132`) and is built on the shared `Field`, `inputClass` and `useDraftNumber` (`:11-14`). _Why —
Note:_ the SOW names four components, not "only four fields"; stated so "built from the shared
inputs" does not overstate. _Verify:_ `swap-panel.tsx:132-178`.

**15. Reduced motion covers the panel and, since #647, the canvas edge packet.** _Note._ The SOW
asks for an "accessibility/mobile viewport pass" (§4.1 l.244), and CLAUDE.md §15 (l.514) says to
honour `prefers-reduced-motion: reduce` and disable edge animations. Before #647 the edge packet
animated through an inline `style` that the `globals.css` reduced-motion rule did not reach. The
packet now carries `className="edge-packet"` (`components/nodes/animated-edge.tsx:60`), which the
reduced-motion block lists (`app/globals.css:676`). _Why — Note:_ CLAUDE.md §15's "disable edge
animations" has held only since #647; the claim is not builder-wide beyond that (note 12).
_Verify:_ `tests/e2e/d3-swap-panel.spec.ts:436-443`: the packet's `animationName` is `none` under
`reduce` and `flow-dash` without it, on both projects.

### SOW starting point, checked against the code

The SOW's description of where the builder stood was written before the code was surveyed on
20 September. These are corrections of fact, not decisions:

- The "two different asset selectors in the same file, adjacent to each other" quoted
  [below](#what-before-and-after-means-here) were two thin wrappers of one component,
  `AssetField` and `AssetSimpleSelect` in `components/builder/config-panel.tsx` (`:2783` and
  `:2802` at [`44928ca`](https://github.com/artisam-paiflow/paiflow/commit/44928ca744f62a7b6f588d992ac5a85bbe3ecd93)).
  The inconsistency was real, since one forwards `error` and one does not (#407), but it was one
  implementation, not two.
- `AddressPicker` was not greenfield. `components/builder/address-input.tsx` (510 lines, with an
  address book and a portal listbox) already existed and was the one input that _was_ shared. D3
  evolved it.
- "Inconsistent validation, duplicated UI code, and confusing error messages" (§3 item 3,
  l.70–72) was accurate, and worse than stated for amounts: the legacy amount inputs lose the
  decimal point (#615).
- "UI tests green in CI" (§5.1 l.285) presumes a UI test lane. None had ever existed; #606 built
  it.
- §3.8's "Swapper config panel uses shared input primitives" (l.200–201) is met literally: all
  four components are rendered in the panel. Notes 1 and 2 record _how_ two of them are used.
- §6.2 lists a "Deliverable 4" (l.312) that does not exist, as D2 noted.

## Props contract

What another node type's panel passes to adopt each component, read from the source in
`components/builder/inputs/`. Every component is controlled: the caller owns the value. The two
numeric inputs hold a draft while the user types and call `onChange` only with a value the flow
schema accepts, clamping on blur and saying so under the field; `AddressPicker` does the same with
`commit="valid"`. `error` is the message to show under the field, wired to `aria-invalid` and
`aria-describedby` through the shared `Field`. Each component has one test with a non-swap props
shape.

**`AssetSelect`** (`asset-select.tsx`) — `label`, `value: Asset`, `onChange(asset)`, `error?`,
`hint?`, `expectedAsset?` (the asset arriving from upstream; options that differ are disabled),
`options?` (defaults to the catalogue), `disabled?`, `onUpstreamCustom?: "adopt" | "error"` (what
to do when upstream delivers an asset outside `options`). Non-swap test:
`tests/unit/builder/inputs/asset-select.test.tsx:149`.

**`AmountInput`** (`amount-input.tsx`) — `label`, `value` (stroops as a string, `""` when unset),
`onChange(stroops)`, `asset?` (the unit suffix and the echo), `min?` and `max?` (stroops, defaulting
to 1 stroop and the largest i128), `decimals?`, `error?`, `hint?`, `disabled?`, `emptyNote?` (the
note shown when a cleared field blurs), `echo?` (the `= 1.5 USDC` line). Non-swap test, a pay
node's amount: `tests/unit/builder/inputs/amount-input.test.tsx:201`.

**`ShareInput`** (`share-input.tsx`) — `label`, `value` (integer basis points), `onChange(bps)`,
`minBps?`, `maxBps?`, `error?`, `hint?`, `disabled?`, `remainingBps?` (what is left of 100% once
every other share is counted; when given, the field shows the remainder and flags an
over-allocation as its error). Non-swap test, one of a splitter's shares:
`tests/unit/builder/inputs/share-input.test.tsx:89`.

**`AddressPicker`** (`address-picker.tsx`) — a union of three shapes:

- **Unlabelled:** the editable props below, with `error?: boolean`, which colours the border while
  the caller renders the message.
- **Labelled:** the editable props, plus `label`, `hint?` and `error?` (a string is shown under the
  field and described; a boolean only colours the border).
- **Pinned:** `pinned: { id, label, network }` and `hint?`. A server-supplied address shown read-only
  with a copy button and an explorer link; the Swapper's router uses it.

The editable props are `value`, `onChange(value)`, `placeholder?`, `pending?`,
`accept?: "account" | "contract" | "either"`, `pendingAllowed?`, `commit?: "always" | "valid"`,
`addressBook?`, `addressBookLoading?`, `addressBookError?`, `onAddressBookChange?` and
`onSelectEntry?`. Non-swap test, an account field with an address book and a picked entry:
`tests/unit/builder/inputs/address-picker.test.tsx:225`.

`PercentBpsInput` (`percent-bps-input.tsx`) is `ShareInput`'s internal core, not a fifth public
component: only `share-input.tsx` imports it.

## What "before and after" means here

The improvement is standardisation, not decoration. When the SOW was written, each node type in
the builder implemented its own inputs, and the page described "two different asset selectors in
the same file, adjacent to each other"; the survey found them to be two wrappers of one component
(see [SOW starting point](#sow-starting-point-checked-against-the-code)), but validation did behave
inconsistently and error messages did vary. The after state is one set of components in the
Swapper panel with one validation path.

A screenshot pair of the same panel looks similar at a glance, so the evidence also shows the
change in the code (the swap markup removed from `config-panel.tsx`, the swapper flow's schemas on
shared primitives, one rule function for the swap rules and one error model) and, for contrast, a
panel that has _not_ been migrated. That contrast is also the argument for adopting these
components across the rest of the builder in a later phase, which this sprint explicitly does not
attempt. [The code-diff note](../evidence/d3/20-code-diff.md) is that evidence: each claim with the
command that proves it.

## Decisions recorded while building

- **The config panel container (#613).** The container is shared by every node type, so its
  keyboard, focus, reduced-motion and docked-sheet changes apply to all of them. The axe scan
  covers the Swapper panel only. The 44px touch-target check covers the Swapper panel and, since
  #661, the editable `AddressPicker`'s controls on the Pay panel, because the Swapper renders only
  the pinned branch and the editable one had never been measured.
- **Chat and sheet on a phone.** Below 768px the config panel docks as a bottom sheet and the Ask AI
  chat is full-width, so only one is shown: selecting a node collapses the chat, and opening the chat
  dismisses the sheet.
- **What axe scans.** `[data-testid="config-panel-container"]`, the region around
  `[data-testid="config-panel"]`, so the header's Close and Delete buttons are scanned too.
- **The builder no longer overflows a phone.** The canvas column's bare `1fr` track grew to the
  toolbar's content width (560px on a 412px Pixel 7 with the D3 test flow; the figure varies with
  what the toolbar holds), which put the docked sheet's buttons off screen. The track is now
  `minmax(0, 1fr)`; desktop is unchanged, since the content already fitted there. At first the
  toolbar rows wrapped on a phone; since #679 the phone header is a single row whose title
  truncates instead.
- **Checked by hand on a real phone** (Playwright's device emulation has no virtual keyboard): on a
  vivo Y22s (Android 14, Chrome), `Max slippage (%)` stays visible above the keyboard and the page
  does not zoom on focus
  ([`13-phone-keyboard-up.jpg`](../evidence/d3/13-phone-keyboard-up.jpg),
  [`13-phone-meta.json`](../evidence/d3/13-phone-meta.json)).
- **The simplified Swapper panel (#670).** After the rebuild the panel was still hard to read: six
  fields at equal weight, two to four lines of help under each, every label and hint in the pink-tinted
  `on-surface-variant`, and the live quote as small grey prose at the bottom. #670 changed its presentation
  only; the `swap` config, validation and contracts are untouched:
  - Labels and hints on the shared inputs use a new neutral token, `on-surface-muted`
    (`#a8a3a6`, `BRAND.md` §2 and §6). Pink is kept for the focused field, the actions and the
    quoted output.
  - Each field has one line of help.
  - The preview amount and the live quote share one **Preview** card, with the expected output as
    the figure ("≈ 1.05 USDC") and the minimum underneath. The deploy review's compact quote is
    unchanged.
  - The router and the deadline sit in a collapsed **Advanced** section, which opens by itself on
    a deadline error or a missing router, so no problem is ever collapsed away.
  - The MiniMap is hidden while a config panel is open. On a phone it covered the docked sheet, and
    on desktop it intercepted clicks on the floating card.
  - The labels `Asset In` and `Asset Out` were kept on purpose, because the validation messages name
    those fields.

  The after-captures (`03`–`08`, `15`–`18`), the recording and the CI test report were all taken
  on a build that includes #670.
