# Nixpacks build hints for Railway

[phases.setup]
nixPkgs = ["nodejs-22_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm db:generate",
  "pnpm build",
]

[start]
cmd = "pnpm db:migrate:deploy && pnpm start"
