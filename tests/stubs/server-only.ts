// Stub for `server-only` so vitest can import server-tagged modules.
// In a real Next.js build the import is replaced with a throwing module on
// the client; here in node, it's a no-op.
export {};
